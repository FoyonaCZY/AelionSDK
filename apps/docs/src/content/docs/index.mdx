---
title: AelionSDK
description: A browser-first SDK for timeline editing, real-time preview, playback and media export.
template: splash
hero:
  title: Build a video editor in the browser
  tagline: AelionSDK owns the timeline, media pipeline, preview, playback and export. You keep control of the product UI.
  actions:
    - text: Build your first project
      link: /AelionSDK/start/getting-started/
      icon: right-arrow
      variant: primary
    - text: Explore capabilities
      link: /AelionSDK/start/capabilities/
      icon: open-book
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## What you can build

AelionSDK is a framework-agnostic browser editing and rendering engine. Register media, load a canonical Project, preview any timeline position, edit through transactions, and export the same project to MP4, WebM, images, GIF or WAV.

<CardGrid>
  <Card title="Edit a timeline">
    Move, trim, split, ripple, roll, slip and slide clips with linked A/V items, markers, keyframes
    and undoable commands.
  </Card>
  <Card title="Preview and play">
    Use Canvas preview, worker compositing, AudioWorklet timing, adaptive quality and cancellation
    of stale frames.
  </Card>
  <Card title="Export locally">
    Run capability preflight before H.264/AAC MP4, VP9/Opus WebM, AV1/HEVC, image, GIF and WAV/RF64
    export.
  </Card>
  <Card title="Extend safely">
    Add custom Materials, proxy media, persistent caches, isolated Worker RPC and remote export
    providers.
  </Card>
</CardGrid>

## Recommended path

1. [Install and configure](/AelionSDK/start/installation/) the workspace or your application.
2. Follow [From a local video to MP4](/AelionSDK/start/getting-started/).
3. Choose the right [packages](/AelionSDK/start/packages/) and run [capability preflight](/AelionSDK/production/capability-preflight/).
4. Connect the Session to your own timeline, Inspector and persistence layer.

:::note[Release status]
AelionSDK `1.0.0-rc.1` is published to npm under the `next` tag. The API may still change before the first stable release.
:::

The [简体中文](/AelionSDK/zh/) site contains the complete Chinese documentation set. Use the language selector in the header to switch at any time.
